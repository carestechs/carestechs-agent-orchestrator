# API Specification

## Overview

The orchestrator exposes a small HTTP surface. Two shapes of endpoint, separated by trust boundary:

1. **Control-plane endpoints** (`/api/*`) — authenticated. Start runs, inspect runs, cancel runs, fetch traces. Called by CLI, CI, or a human operator.
2. **Webhook endpoints** (`/hooks/*`) — authenticated via HMAC request signature. Called by `carestechs-flow-engine` to report node lifecycle events.

A third entry point — the **CLI** — exists and is the primary operator interface in v1, but it is not part of the HTTP API. The CLI is a thin adapter over the same services the routes call (per the profile's "two entry points, one core" convention).

All endpoints return JSON. All requests and responses validate through Pydantic. OpenAPI docs are auto-generated by FastAPI and served at `/docs`.

### Key API Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Base path (control plane) | `/api/v1` | Versioned under `/api/` per profile convention |
| Base path (webhooks) | `/hooks/engine` | Distinct prefix makes trust boundary obvious; different auth |
| Auth (control plane) | API key via `Authorization: Bearer <token>` | v1 is single-operator local use; JWT is overkill. Swap to `adrs/api/jwt-bearer-auth.md` if multi-user arrives |
| Auth (webhooks) | HMAC-SHA256 signature in `X-Engine-Signature` header over raw request body + shared secret | Mirrors AD-2 in `ARCHITECTURE.md` |
| Response envelope | `{ data, meta }` per `adrs/api/rest-envelope.md` | Profile default |
| Error format | RFC 7807 Problem Details | Matches profile error conventions |
| Pagination | Offset (`page` + `pageSize`) per `adrs/api/offset-pagination.md` | Runs/steps are list resources with bounded cardinality |
| Idempotency (webhooks) | Enforced by `WebhookEvent.dedupe_key` uniqueness (per data model) | Engine retries are safe |
| Long-running calls | Non-blocking — control-plane POSTs return immediately with a run id; progress is observed by polling `GET /runs/{id}` or by a separate trace stream | Matches AD-2: the engine completes via webhook, not sync response |

## Common Conventions

### Response Envelope

```json
{
  "data": { },
  "meta": {
    "totalCount": 0,
    "page": 1,
    "pageSize": 20
  }
}
```

Single-resource responses omit `meta`. Collection responses always include it.

### Error Response (RFC 7807)

```json
{
  "type": "https://orchestrator.local/problems/validation-error",
  "title": "Validation error",
  "status": 400,
  "detail": "`intake.feature_brief_path` is required",
  "errors": {
    "intake.feature_brief_path": ["field required"]
  }
}
```

### Authentication

- **Control plane:** `Authorization: Bearer <api-key>`. API key is a server-side secret configured via env var `ORCHESTRATOR_API_KEY`. Missing/invalid → `401`.
- **Webhooks:** HMAC-SHA256 over the raw request body, keyed by `ENGINE_WEBHOOK_SECRET`, submitted as `X-Engine-Signature: sha256=<hex>`. Missing/mismatched → `401` and the event is persisted with `signature_ok=false`.

### Pagination

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| page | int | 1 | Page number (1-based) |
| pageSize | int | 20 | Items per page (max 100) |

## Endpoints by Module

### `ai` module

#### Runs

##### POST /api/v1/runs

> *Start a new agent run. Returns immediately with the run id; progress is observed via `GET /api/v1/runs/{id}`.*

| Attribute | Value |
|-----------|-------|
| **Auth** | Required |
| **Roles** | Any (single-operator v1) |

**Request Body:**

```json
{
  "agentRef": "string — stable agent reference, e.g. 'lifecycle-agent@0.3.0'",
  "intake": {
    "featureBriefPath": "string — optional, e.g. 'docs/work-items/FEAT-042.md'",
    "…": "other agent-specific intake fields (validated by the loaded agent definition)"
  },
  "budget": {
    "maxSteps": "int — optional, overrides agent-definition default",
    "maxTokens": "int — optional"
  }
}
```

**Response (202 Accepted):**

```json
{
  "data": {
    "id": "uuid",
    "agentRef": "string",
    "status": "pending",
    "startedAt": "timestamptz",
    "traceUri": "string"
  }
}
```

**Status Codes:**

| Code | Condition |
|------|-----------|
| 202 | Run accepted and queued |
| 400 | Validation error (unknown `agentRef`, invalid intake against agent schema) |
| 401 | Unauthorized |
| 429 | Rate-limited (concurrent run cap) |

---

##### GET /api/v1/runs

> *List runs with pagination and filtering.*

| Attribute | Value |
|-----------|-------|
| **Auth** | Required |

**Query Parameters:**

| Name | Type | Description |
|------|------|-------------|
| `status` | enum `RunStatus` | Optional filter |
| `agentRef` | string | Optional filter |
| `page` | int | See pagination |
| `pageSize` | int | See pagination |

**Response (200 OK):**

```json
{
  "data": [
    {
      "id": "uuid",
      "agentRef": "string",
      "status": "running",
      "stopReason": null,
      "startedAt": "timestamptz",
      "endedAt": null
    }
  ],
  "meta": { "totalCount": 42, "page": 1, "pageSize": 20 }
}
```

---

##### GET /api/v1/runs/{id}

> *Fetch a single run with its latest status and summary.*

| Attribute | Value |
|-----------|-------|
| **Auth** | Required |

**Response (200 OK):**

```json
{
  "data": {
    "id": "uuid",
    "agentRef": "string",
    "agentDefinitionHash": "string",
    "intake": { },
    "status": "running",
    "stopReason": null,
    "startedAt": "timestamptz",
    "endedAt": null,
    "traceUri": "string",
    "stepCount": 7,
    "lastStep": {
      "id": "uuid",
      "stepNumber": 7,
      "nodeName": "string",
      "status": "in_progress"
    }
  }
}
```

**Status Codes:** `200`, `401`, `404`.

---

##### POST /api/v1/runs/{id}/cancel

> *Request cancellation of a running run. Idempotent — cancelling a terminal run is a no-op returning the current state.*

| Attribute | Value |
|-----------|-------|
| **Auth** | Required |

**Request Body:**

```json
{ "reason": "string — optional free-text" }
```

**Response (200 OK):**

```json
{
  "data": {
    "id": "uuid",
    "status": "cancelled",
    "stopReason": "cancelled",
    "endedAt": "timestamptz"
  }
}
```

**Status Codes:** `200`, `401`, `404`, `409` (conflict — cancellation rejected, e.g., run already terminal with different reason — rare).

---

#### Run Trace

##### POST /api/v1/runs/{id}/signals

> *Operator-injected signal for an in-flight run. v1 supports only `name="implementation-complete"`; the lifecycle agent uses it to resume a run paused at `wait_for_implementation`. Introduced by FEAT-005.*

| Attribute | Value |
|-----------|-------|
| **Auth** | Required |

**Request Body:**

```json
{
  "name": "implementation-complete",
  "taskId": "T-001",
  "payload": {
    "commit_sha": "string — optional",
    "notes": "string — optional"
  }
}
```

**Response (202 Accepted):**

```json
{
  "data": {
    "id": "uuid",
    "runId": "uuid",
    "name": "implementation-complete",
    "taskId": "T-001",
    "payload": {},
    "receivedAt": "timestamptz",
    "dedupeKey": "sha256-hex"
  },
  "meta": { "alreadyReceived": true }
}
```

The `meta` object is omitted on first delivery; subsequent calls with the same `(run_id, name, task_id)` return the same `data` and `meta.alreadyReceived=true`. The supervisor is not re-woken on duplicate calls.

**Status Codes:**

| Code | Condition |
|------|-----------|
| 202 | Signal persisted and (on first delivery) supervisor woken |
| 400 | Unknown `name` value (v1 allows `implementation-complete` only) |
| 401 | Unauthorized |
| 404 | Run not found or `taskId` not in run memory |
| 409 | Run already terminal (signal persisted, supervisor not woken) |

**Ordering invariant:** the endpoint persists the `RunSignal` row before calling `supervisor.deliver_signal(...)`. The trace records one entry of kind `operator_signal` per first delivery (idempotent — no duplicate trace entries).

---

##### GET /api/v1/runs/{id}/steps

> *List steps for a run in sequence order.*

| Attribute | Value |
|-----------|-------|
| **Auth** | Required |

**Query Parameters:** `page`, `pageSize`.

**Response (200 OK):**

```json
{
  "data": [
    {
      "id": "uuid",
      "stepNumber": 1,
      "nodeName": "generate_tasks",
      "status": "completed",
      "nodeInputs": { },
      "nodeResult": { },
      "error": null,
      "dispatchedAt": "timestamptz",
      "completedAt": "timestamptz"
    }
  ],
  "meta": { "totalCount": 12, "page": 1, "pageSize": 20 }
}
```

---

##### GET /api/v1/runs/{id}/policy-calls

> *List policy decisions for a run. Each entry is a full record of what the model was asked and what it chose.*

| Attribute | Value |
|-----------|-------|
| **Auth** | Required |

**Response (200 OK):**

```json
{
  "data": [
    {
      "id": "uuid",
      "stepId": "uuid",
      "provider": "anthropic",
      "model": "claude-opus-4-6",
      "selectedTool": "generate_tasks",
      "toolArguments": { },
      "availableTools": [
        { "name": "generate_tasks", "description": "…", "parameters": { } }
      ],
      "inputTokens": 4213,
      "outputTokens": 289,
      "latencyMs": 1847,
      "createdAt": "timestamptz"
    }
  ],
  "meta": { "totalCount": 12, "page": 1, "pageSize": 20 }
}
```

---

##### GET /api/v1/runs/{id}/trace

> *Stream the run's full trace as NDJSON (one JSON object per line). Merges steps, policy calls, and webhook events in chronological order. Useful for offline inspection.*

| Attribute | Value |
|-----------|-------|
| **Auth** | Required |
| **Content-Type** | `application/x-ndjson` |

**Response (200 OK):** Streaming NDJSON, each line shaped `{ "kind": "step"|"policy_call"|"webhook_event", "at": "timestamptz", ... }`.

---

#### Agents

##### GET /api/v1/agents

> *List the agent definitions currently discoverable on disk.*

| Attribute | Value |
|-----------|-------|
| **Auth** | Required |

**Response (200 OK):**

```json
{
  "data": [
    {
      "ref": "lifecycle-agent@0.3.0",
      "definitionHash": "string",
      "path": "agents/lifecycle-agent.yaml",
      "intakeSchema": { },
      "availableNodes": ["generate_tasks", "generate_plan", "..."]
    }
  ],
  "meta": { "totalCount": 1, "page": 1, "pageSize": 20 }
}
```

Agent definitions are files on disk in v1 — this endpoint reads them, it does not manage them. No POST/PUT/DELETE counterpart.

---

### Webhook endpoints (ingress from flow engine)

#### POST /hooks/engine/events

> *Single ingress for all flow-engine lifecycle events. The engine posts one event per request.*

| Attribute | Value |
|-----------|-------|
| **Auth** | HMAC-SHA256 signature over raw body via `X-Engine-Signature` |
| **Idempotency** | Enforced by `dedupeKey` |

**Request Headers:**

| Header | Required | Description |
|--------|----------|-------------|
| `X-Engine-Signature` | Yes | `sha256=<hex>` of HMAC(ENGINE_WEBHOOK_SECRET, raw-body) |
| `X-Engine-Event-Id` | Yes | Stable per-event id from the engine (component of `dedupeKey`) |

**Request Body:**

```json
{
  "eventType": "node_started | node_finished | node_failed | flow_terminated",
  "engineRunId": "string",
  "engineEventId": "string — unique per logical event on the engine side",
  "stepCorrelationId": "uuid — matches Step.id on our side",
  "occurredAt": "timestamptz",
  "payload": {
    "result": "…only on node_finished…",
    "error":  "…only on node_failed…"
  }
}
```

**Response (202 Accepted):**

```json
{ "data": { "received": true, "eventId": "uuid" } }
```

**Status Codes:**

| Code | Condition |
|------|-----------|
| 202 | Event accepted (including deduplicated retries) |
| 400 | Malformed payload |
| 401 | Missing or invalid signature (event still persisted with `signatureOk=false`) |
| 404 | `engineRunId` / `stepCorrelationId` does not match a known run/step |
| 422 | Signature valid, but payload fails domain validation |

---

### Service endpoints

#### GET /health

> *Liveness and dependency readiness. Unauthenticated.*

**Response (200 OK):**

```json
{
  "data": {
    "status": "ok",
    "checks": {
      "database": "ok",
      "llm_provider": "ok",
      "flow_engine": "ok"
    }
  }
}
```

## Shared DTOs

### RunSummaryDto

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | uuid | No | Run id |
| agentRef | string | No | Agent reference |
| status | enum `RunStatus` | No | Current status |
| stopReason | enum `StopReason` | Yes | Populated when terminal |
| startedAt | timestamptz | No | Loop start |
| endedAt | timestamptz | Yes | Populated when terminal |

### StepDto

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | uuid | No | Step id |
| stepNumber | int | No | 1-based, contiguous per run |
| nodeName | string | No | Engine-side node chosen |
| status | enum `StepStatus` | No | See data model |
| nodeInputs | object | No | Policy tool-call arguments |
| nodeResult | object | Yes | Success payload |
| error | object | Yes | Failure payload |
| dispatchedAt | timestamptz | Yes | Engine accepted dispatch |
| completedAt | timestamptz | Yes | Terminal webhook processed |

### PolicyCallDto

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | uuid | No | Policy call id |
| stepId | uuid | No | 1:1 with a step |
| provider | string | No | e.g. `anthropic` |
| model | string | No | Concrete model id |
| selectedTool | string | No | Tool name the model called |
| toolArguments | object | No | Tool-call arguments |
| availableTools | array<object> | No | Action space on this call |
| inputTokens | int | No | Provider-reported |
| outputTokens | int | No | Provider-reported |
| latencyMs | int | No | End-to-end provider latency |
| createdAt | timestamptz | No | Record creation |

### RunSignalDto

*FEAT-005 — operator-injected signal.*

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | uuid | No | Signal id |
| runId | uuid | No | Parent run |
| name | string | No | Signal name (v1: `"implementation-complete"`) |
| taskId | string | Yes | Target task when scoped; NULL for run-scoped signals |
| payload | object | No | Freeform payload (e.g. `commit_sha`, `notes`) |
| receivedAt | timestamptz | No | When the HTTP request landed |
| dedupeKey | string | No | Deterministic hash for idempotent retries |

### WebhookEventDto

| Field | Type | Nullable | Description |
|-------|------|----------|-------------|
| id | uuid | No | Event id |
| eventType | enum `WebhookEventType` | No | See data model |
| engineRunId | string | No | Correlation id |
| payload | object | No | Validated body |
| signatureOk | bool | No | HMAC validation outcome |
| receivedAt | timestamptz | No | HTTP arrival |
| processedAt | timestamptz | Yes | Runtime consumed it |

## Endpoint Summary

| Method | Path | Module | Auth | Description |
|--------|------|--------|------|-------------|
| POST | `/api/v1/runs` | ai | Required | Start a new run (returns 202) |
| GET | `/api/v1/runs` | ai | Required | List runs |
| GET | `/api/v1/runs/{id}` | ai | Required | Fetch a run summary |
| POST | `/api/v1/runs/{id}/cancel` | ai | Required | Cancel a run |
| POST | `/api/v1/runs/{id}/signals` | ai | Required | Deliver an operator signal (FEAT-005) |
| GET | `/api/v1/runs/{id}/steps` | ai | Required | List steps of a run |
| GET | `/api/v1/runs/{id}/policy-calls` | ai | Required | List policy decisions of a run |
| GET | `/api/v1/runs/{id}/trace` | ai | Required | Stream run trace as NDJSON |
| GET | `/api/v1/agents` | ai | Required | List on-disk agent definitions |
| POST | `/hooks/engine/events` | ai | HMAC | Engine event ingress |
| GET | `/health` | (service) | Public | Liveness + dependency checks |

## AI Task Generation Notes

- **Controller structure:** All control-plane endpoints live in the `ai` module's `router.py`; the webhook endpoint may live in the same router under a distinct prefix (`/hooks/engine`). Do NOT create a second module for webhooks — they are still `ai` concerns.
- **DTO generation:** Response DTOs map 1:1 to the Shared DTOs section. Pydantic models MUST use camelCase JSON aliases (per profile convention), though Python attributes remain snake_case.
- **Long-running calls:** `POST /api/v1/runs` MUST return `202 Accepted` immediately, never block waiting for the agent loop to finish. Any task that makes it synchronous violates AD-2.
- **Idempotent ingress:** Webhook handlers MUST consult `WebhookEvent.dedupe_key` before side-effecting. Any task that skips dedupe is rejected.
- **Signature verification:** MUST happen *before* payload parsing for `/hooks/engine/*`, but the raw body MUST be persisted regardless (for forensics) when signature fails.
- **Response envelope:** All 2xx JSON responses use `{ data, meta? }`. Error responses use RFC 7807 — NEVER mix envelope and error shapes.
- **OpenAPI hygiene:** Every endpoint MUST have a Pydantic response model so FastAPI's generated OpenAPI is accurate. NEVER return `dict` / `Any`.

## Changelog

- 2026-04-18 — FEAT-005 — `POST /runs/{id}/signals` is live: operator-injected signals (v1: `name="implementation-complete"`), persist-first-then-wake, idempotent on `(run_id, name, task_id)`. Added `RunSignalDto` under Shared DTOs. Trace stream surfaces a new `kind=operator_signal` entry (filterable via `?kind=operator_signal`).
- 2026-04-18 — FEAT-004 — `GET /runs/{id}/trace` is now live: returns `application/x-ndjson` (one record per line, shape `{"kind":..., "data":<DTO>}`), supports `?follow=true`, `?since=<ISO-8601>`, and repeatable `?kind=` filters. Unknown run id → `404` Problem Details before any bytes are written. `Cache-Control: no-cache` and `X-Accel-Buffering: no` set on the response. Reserved contract from FEAT-001 unchanged.
- 2026-04-18 — FEAT-002 — Control-plane read/write endpoints replaced their 501 stubs with real implementations backed by the runtime loop. `POST /runs/{id}/cancel` is idempotent on already-terminal runs. `GET /runs/{id}/trace` remains deferred to FEAT-004. No contract changes.
- 2026-04-15 — Initial version. Defined control-plane endpoints for runs, steps, policy calls, trace, agents; defined webhook ingress; documented HMAC auth for webhooks and Bearer for control plane; locked 202-Accepted async contract for run starts.
